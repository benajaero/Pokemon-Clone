#ifndef ENUM_H
#define ENUM_H

enum Gender {
    MALE,
    FEMALE
};

//clockwise
enum Orientation {
    NORTH,
    EAST,
    SOUTH,
    WEST
};

enum BattleType {
    WILD,
    TRAINER,
    COMPETITION
};

#endif
